import { useState } from "react";

export default function modal(){

    var modalstate = false;

    function toggleFlag(){
        // modalstate = value ? false : true;
        // return {modalstate};
        console.log(modalstate)
     }

    return toggleFlag;
}